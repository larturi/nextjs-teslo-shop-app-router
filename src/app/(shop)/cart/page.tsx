import { titleFont } from '@/config/fonts'

export default function CartPage() {
  return (
    <div className=''>
      <h1 className={`${titleFont.className} font-bold`}>Cart Page</h1>
    </div>
  )
}
